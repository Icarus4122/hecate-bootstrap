# pipx-packages.txt — Python tools installed via pipx in kali-main.
# Consumed by: docker/kali-main/Dockerfile
#
# Keep short.  Do not duplicate tools already installed via apt
# (netexec, impacket-scripts, evil-winrm, enum4linux, etc.).

uploadserver
